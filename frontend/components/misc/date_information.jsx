import React from 'react';


export const totalCount = function(dates){
    const datesLength = Object.keys(dates).length
    return datesLength
}





export const uniqueDateType = function(dates) {

}
export const uniqueDateLocation = function(dates) {
    let keys = []
    let values = []
    if (dates) {
        keys = Object.keys(dates)
        values = Object.values(dates)
    }
}

