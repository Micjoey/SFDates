import React, { useState } from 'react'
import renderModal from './modal_render';


export const filterModal = (type, id, menu) => {
    if (!type || !id) return null;
    const [isShowing, toggle] = useState(false);
    

   
}


export const useModal = (isShowing, type, id, menu) => {

}
